<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>AI記事生成アプリ</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; line-height: 1.6; }
    h1 { color: #2c3e50; }
    h2 { margin-top: 1.5rem; color: #34495e; }
    .article { margin-top: 2rem; }
    .form-container { margin-bottom: 2rem; }
  </style>
</head>
<body>
  <div class="container">
  <div class="form-container">
    <form action="/generate" method="POST">
      <label for="keyword">キーワード</label>
        <input type="text" id="keyword" name="keyword" placeholder="キーワードを入力" required>

        <label for="competitorUrl1">競合記事URL 1</label>
        <input type="text" id="competitorUrl1" name="competitorUrl1" placeholder="https://example.com/article1">

        <label for="competitorUrl2">競合記事URL 2</label>
        <input type="text" id="competitorUrl2" name="competitorUrl2" placeholder="https://example.com/article2">

        <label for="competitorUrl3">競合記事URL 3</label>
        <input type="text" id="competitorUrl3" name="competitorUrl3" placeholder="https://example.com/article3">

        <button type="submit">記事生成</button>
    </form>
  </div>
    <h1><%= title %></h1>

    <% if (title) { %>
      <h1><%= title %></h1>
      <div class="article">
        <% sections.forEach(section => { %>
          <div class="section"><%= section.body.replace(/\n/g, '<br>') %></div>
        <% }) %>
      </div>
    <% } %>
    
  </div>
</body>
</html>
